{
  "verify": {
    "commands": ["make verify"]
  },
  "build": {
    "test": {
      "commands": ["make build"],
      "buildDir": "./built"
    },
    "prod": {
      "commands": ["make build"],
      "buildDir": "./built"
    }
  },
  "tests": {
    "test": [
      {
        "name": "End to End Tests",
        "commands": ["make test-e2e"]
      }
    ],
    "prod": [
      {
        "name": "Smoke Tests",
        "commands": ["make test-smoke"]
      }
    ]
  }
}
